# =============================================================================
# OSC Market Intelligence App - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real credentials to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Databricks Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Your Databricks workspace URL
DATABRICKS_HOST=https://e2-demo-field-eng.cloud.databricks.com

# Your Databricks personal access token
# Get this from: User Settings > Developer > Access Tokens > Generate New Token
DATABRICKS_TOKEN=your_token_here

# The serving endpoint name to call
ENDPOINT_NAME=mas-1ab024e9-endpoint

# -----------------------------------------------------------------------------
# Lakebase/PostgreSQL Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# If you want conversation history, configure a database
# The app works without this, but conversations won't be persisted

# Database host (e.g., your-lakebase-instance.postgres.database.azure.com)
DB_HOST=your_lakebase_host

# Database port (default: 5432)
DB_PORT=5432

# Database name
DB_NAME=market_intelligence

# Database username
DB_USER=your_db_user

# Database password
DB_PASSWORD=your_db_password

# -----------------------------------------------------------------------------
# Optional Settings
# -----------------------------------------------------------------------------
# Add any additional configuration here as needed

